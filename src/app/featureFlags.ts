/**
 * The redesigned home is isolated behind one switch so the legacy home can be
 * restored without touching navigation, practice state, or route pages.
 */
export const HOME_REDESIGN_V2_ENABLED = true

/**
 * The visual theme layer only changes presentation on approved screens. Turn
 * this off to restore the existing palette without touching routes or state.
 */
export const VISUAL_THEME_ENABLED = true
