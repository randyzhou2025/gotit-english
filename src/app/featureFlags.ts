/**
 * The redesigned home is isolated behind one switch so the legacy home can be
 * restored without touching navigation, practice state, or route pages.
 */
export const HOME_REDESIGN_V2_ENABLED = true
